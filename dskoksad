Wycofanie zakończone — **gate zielony**:

- **Keeper** `JacksonDeserializationRegressionTest`: `Tests run: 3, Failures: 0` (Relationship + ResponseErrorDTO przez `RelationshipResponseDTO`, `GetAgreementResponseDto`, oraz probe `{}` na 3 celach). `Agreement` usunięty.
- **Auto-guard** `FeignAndRequestBodyDeserializationGuardTest`: `Tests run: 1` — **24 auto-wykryte cele, RED: 0**.
- **BUILD SUCCESS.**

## Podsumowanie wykonanych kroków

**1. Potwierdzenie martwego kodu** — pełny grep referencji `domainaservices.agreements.model.Agreement`:
- `src/main` **nie konstruuje** naszej `Agreement` przez konstruktor bezargumentowy. Trafienie `AgreementsSourceChoosingService:48 new Agreement(...)` to false-positive — używa `customerparty.model.Agreement` (record, import linia 13).
- Naszą `Agreement` używają **tylko testy**, i to przez konstruktor cząstkowy 3-arg, nie bezargumentowy → czysto do wycofania.

**2. Cofnięto fix** w `Agreement.java`: usunięto `@NoArgsConstructor` + `import lombok.NoArgsConstructor;` → powrót do `@Data @AllArgsConstructor`.

**3. Wyczyszczono keeper**: usunięto metodę `deserializesAgreement()`, import `Agreement`, linię `Agreement.class` z `deserializesEmptyObjectsForAllFixedTargets`, zaktualizowano javadoc.

**4. Gate zielony** (powyżej).

## Finalny, czysty stan

**Fix (3 żywe cele deserializacji)** — pozostają z `@NoArgsConstructor`:
- `Relationship` (zagnieżdżony w `RelationshipResponseDTO`)
- `ResponseErrorDTO` (zagnieżdżony w errorDetails)
- `GetAgreementResponseDto` (return type `CisAgreementClient.getAgreements()`)

**Guardy (2 testy)**: keeper z asercjami wartości + auto-odkrywający guard (24 cele, rośnie z kodem).

Nic nie pushowane/commitowane. `jackson-deser-fix.diff` jest teraz nieaktualny — daj znać, jeśli mam go zregenerować.
