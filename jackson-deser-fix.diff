Fix i test JacksonDeserializationRegressionTest zatwierdzone — czyste. Zanim uznamy "zero niezałatanych", domknij dwie luki w klasyfikacji LOW RISK, i przerób guard tak, żeby rósł z kodem.

LUKA 1 — cache (ConsentCacheDTO, CachedObject): "cache" nie znaczy "tylko wychodzące" — odczyt z cache to deserializacja. Ustal:
- jakim serializerem cache zapisuje/czyta te DTO (Redis GenericJackson2JsonRedisSerializer? JDK serialization? in-memory obiektowo?),
- jeśli to JSON przez mapper Jacksona 3 → te DTO SĄ celami deserializacji, sprawdź czy mają no-arg ctor/creator i czy odczyt z cache faktycznie się deserializuje (test round-trip jak dla czwórki),
- jeśli to OSOBNY mapper Jacksona 2 (com.fasterxml) albo nie-JSON → odporne, ale pokaż dowód (konfigurację CacheManagera/serializera), nie samo twierdzenie.

LUKA 2 — konsumenci kolejek: producent MORS jest wychodzący (writeValueAsString + convertAndSend), to wiemy. Potwierdź, czy istnieje jakikolwiek KONSUMENT (listener) deserializujący payload przez Jacksona — a nie przez MorsDictionaryAspect/refleksję. To jest założenie dźwigające odrzucenie CISAgreement/VasOffer i całej rodziny modeli MORS. Pokaż listenery i jak budują obiekt z wiadomości.

GUARD — przerób z zahardkodowanej listy na auto-odkrywający (to zamyka ślepą plamkę na przyszłe DTO):
- zostaw JacksonDeserializationRegressionTest jako czytelny keeper dla czwórki (z asercjami wartości — zostaje bez zmian),
- ale zamiast kasować scan, ZAWĘŹ jego scope do realnych celów deserializacji: przez refleksję zbierz typy zwracane przez interfejsy klientów Feign + typy parametrów @RequestBody z kontrolerów (+ payloady konsumentów kolejek z LUKI 2, jeśli jakieś deserializują przez Jacksona), i dla każdego zrób sondę readValue("{}", typ) przez prawdziwy mapper Boot 4. To ma być zielone dla wszystkich realnych celów i automatycznie obejmować nowe DTO. 133 RID ze starego scana znikają, bo nie skanujesz już @Service/@Component/modeli domenowych — tylko to, co faktycznie wchodzi z JSON-a.
- jeśli auto-odkrycie celów Feign/@RequestBody jest niewykonalne w rozsądnym czasie, powiedz to wprost i zostaw keeper + jawnie nazwij residual gap (nowe DTO nieobjęte) — żeby to była świadoma decyzja, nie cicha dziura.

STOP, pokaż: werdykt cache (z dowodem serializera), werdykt konsumentów kolejek (z listenerami), i nowy zawężony guard (które typy auto-wykrył, wszystkie zielone?). Nie pushuj.
