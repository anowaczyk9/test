Werdykty cache i kolejek przyjęte — dowody solidne (in-memory object stores + grep zero Redis; listener na record przez Jackson2JsonMessageConverter). Guard auto-odkrywający zostaje, świetna robota — to on wyłapał, że nasz fix pomylił dwie klasy o tej samej nazwie.

Decyzja co do Agreement: WYCOFAJ fix i asercję — diff ma być ściśle „żywe cele deserializacji" (3), nie defensywne dotknięcia martwego kodu.

Wykonaj:
1. Najpierw potwierdź, że nic w src/main nie konstruuje domainaservices.agreements.model.Agreement przez konstruktor bezargumentowy (pełny grep referencji, nie tylko cele deser). Jeśli używa go TYLKO test — czysto do wycofania. Jeśli coś w src/main na nim polega — STOP i pokaż, zanim tkniesz.
2. Cofnij @NoArgsConstructor (+ import) z domainaservices.agreements.model.Agreement — wróć do stanu sprzed fixa.
3. Usuń z JacksonDeserializationRegressionTest metodę deserializesAgreement (i Agreement z deserializesEmptyObjectsForAllFixedTargets, jeśli tam jest). Keeper zostaje na 3 realnych celach: Relationship, ResponseErrorDTO, GetAgreementResponseDto.
4. Gate: potwierdź, że po wycofaniu wszystko dalej zielone (keeper + auto-guard 24 cele).
